<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de Fundo Suave -->
    <solid android:color="#F0F8FF" />

    <!-- Bordas Arredondadas -->
    <corners android:radius="12dp" />

    <!-- Borda (Opcional, para dar destaque) -->
    <stroke
        android:width="1dp"
        android:color="#D3D3D3" />

    <!-- Padding Interno (para dar espaço ao texto) -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" />
</shape>
